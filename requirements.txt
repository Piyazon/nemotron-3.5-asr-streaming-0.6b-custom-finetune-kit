# =============================================================================
# Nemotron 3.5 ASR Fine-Tuning — Requirements
# =============================================================================
# System dependencies (install with apt, not pip):
#   sudo apt-get install ffmpeg sox libsndfile1 libsox-fmt-mp3
#
# CUDA note: Training is Linux/NVIDIA-only. Numba's CUDA target is now shipped
#            separately as numba-cuda; use its CUDA 12 dependency set below.
# =============================================================================

# --- Core ML / Hugging Face audio decoding ---
# Hugging Face Datasets 4.4+ decodes Audio columns through TorchCodec's
# AudioDecoder API. TorchCodec has a release-specific PyTorch ABI, so keep
# these on the officially compatible PyTorch 2.8 / TorchCodec 0.7 pair.
torch==2.8.*
torchcodec==0.7.*
datasets[audio]>=4.4.0,<5.0

# --- NeMo ASR (installs warprnnt, lhotse, sentencepiece, hydra, omegaconf, etc.) ---
nemo_toolkit[asr] @ git+https://github.com/NVIDIA/NeMo.git@main

# --- Explicit dependency for automatic custom-language BPE generation ---
sentencepiece>=0.2.0

# --- NVIDIA Numba CUDA target used by NeMo's RNNT kernels ---
numba-cuda[cu12] ; platform_system == "Linux"

# --- Hugging Face Hub (for downloading pretrained models) ---
huggingface-hub

# --- Audio / Data helpers ---
librosa
soundfile

# --- Utilities ---
Cython
matplotlib
text-unidecode
wget
